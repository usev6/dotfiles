# Yet another repository with dotfiles

Here are those of my dotfiles which I want to use on multiple machines.

## install

```sh
git clone https://github.com/usev6/dotfiles.git ~/dotfiles
./dotfiles/deploy_dotfiles
```
